import React from 'react';
import Table from '../../components/common/Table';

const ListView = ({ data }) => {
    return (
        <div>
            {/* <Table data={data} /> */}
        </div>
    );
};

export default ListView;